import styled from "@emotion/styled";

const LayoutBanner = () => {
  const Wrapper = styled.div`
    height: 100px;
    background-color: yellow;
  `;
  return <Wrapper>여기는 배너영역입니다. Hellow World!</Wrapper>;
};

export default LayoutBanner;
