import styled from "@emotion/styled";

const LayoutFooter = () => {
  const Wrapper = styled.div`
    height: 50px;
    background-color: gray;
  `;
  return <Wrapper>여기는 푸터영역입니다.</Wrapper>;
};

export default LayoutFooter;
